import React from 'react';
import AuthForm from '../Auth/AuthForm';

const Navbar = () => {
  return (
    <div>
      {/* Other navbar content */}
      <AuthForm />
    </div>
  );
};

export default Navbar;
